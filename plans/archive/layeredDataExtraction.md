now we have verified that given the hit.SetObject.Index we can index into the triangleset. the triangleset is built in loadTrinagles' in "C:\Users\haral\Desktop\pro3d\pro3d-6\src\PRo3D.Core\Surface.fs" there the positions are loaded to construct the array. what i now want is to load other attributes which are stored by patch (e.g. gravity values, altitude values etc). the have the same layout as the vertices (positions.Data). per sampling point i now want to extract the additional parameters available in this patch. In patchFileInfo always knows the name of the vertices (positions), other attributes are besides this file (E.g. gravity etc) ending in ara with the same layout. i think we need to discover all attributes available and extract all of them