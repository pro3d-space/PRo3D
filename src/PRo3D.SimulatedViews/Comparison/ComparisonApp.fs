namespace PRo3D.Comparison

[<CompilationRepresentation(CompilationRepresentationFlags.ModuleSuffix)>]
module ComparisonApp =
    let compareMeasurements (m : ComparisonApp) =
        ()  

    let update (m : ComparisonApp) (msg : ComparisonAction) =
        ()

    let view (m : AdaptiveComparisonApp) =
        ()
        // Html.Layout.boxH [ CustomGui.dropDown Interactions.hideSet m.interaction SetInteraction ]
        // Html.Layout.boxH [ CustomGui.dropDown Interactions.hideSet m.interaction SetInteraction ]
